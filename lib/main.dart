import 'package:flutter/material.dart';
import 'package:gift_planner/app/view/app.dart'; // Import our new App widget

// The main entry point of the entire application.
// This function should be as simple as possible.
void main() {
  // It just needs to run our root widget.
  runApp(const GiftPlannerApp());
}
